#!/usr/bin/env sh
set -eu
ROOT=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
PYTHON="$ROOT/.venv/bin/python"
if [ ! -x "$PYTHON" ]; then
  echo "Shared environment not found. Run scripts/bootstrap.sh first." >&2
  exit 1
fi
exec "$PYTHON" "$@"
