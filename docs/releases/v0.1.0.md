# v0.1.0 tag record — no release published

The immutable `v0.1.0` tag was the first release attempt. GitHub rejected the workflow before starting a job because the workflow used the `runner` context at job-level `env`, where that context is unavailable. No GitHub Release or assets were created.

The tag was preserved and not moved or reused. Release recovery added workflow expression linting to ordinary CI, corrected the context scope, and advanced the package to `v0.1.1`.

Incident evidence: GitHub Actions run `33230433174` and issue #9.
