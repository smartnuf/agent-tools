[build-system]
requires = ["hatchling==1.32.0"]
build-backend = "hatchling.build"

[project]
name = "agent-tools-extraneous-test"
version = "0.0.0"

[tool.hatch.build.targets.wheel]
packages = ["src/agent_tools_extraneous"]
